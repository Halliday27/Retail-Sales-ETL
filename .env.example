# Data source options: local or s3
SOURCE_MODE=local
LOCAL_DATA_DIR=data
S3_BUCKET=my-retail-etl-project
S3_PREFIX=raw

# Example (RDS PostgreSQL)
DATABASE_URL=postgresql://username:password@your-rds-endpoint:5432/retail_dw
